# e-commerce-dashboard
# 1. Unzip the folder
unzip ecommerce-dashboard.zip
cd ecommerce-dashboard

# 2. Create a new repo on github.com (name: ecommerce-dashboard)
#    Don't add README/gitignore — repo must be empty

# 3. Add remote & push
git remote add origin https://github.com/vipulsingh/ecommerce-dashboard.git
git push -u origin main
